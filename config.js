// Fill these in with your own Supabase project's values.
// Find them in your Supabase dashboard under Settings -> API.
//
// These are the "anon public" values, meant to be used in client-side
// code like this — they are safe to commit. Access to your data is
// controlled separately by the Row Level Security policies defined in
// supabase-schema.sql, not by keeping this key secret.
window.SUPABASE_CONFIG = {
  url: 'https://YOUR-PROJECT-REF.supabase.co',
  anonKey: 'YOUR-ANON-PUBLIC-KEY'
};
